# Эмулятор сборщика мусора
Консольное приложение на Java для демонстрации работы сборщика мусора с использованием алгоритма Mark-and-Sweep.

## Описание
Программа эмулирует работу системы управления памятью и автоматической сборки мусора. Реализован классический алгоритм Mark-and-Sweep, который состоит из двух фаз:
- **Mark** — маркировка всех достижимых объектов от корневых
- **Sweep** — удаление недостижимых (мусорных) объектов

# Компиляция
javac GarbageCollectorEmulator.java

# Запуск
java GarbageCollectorEmulator

## Возможности
- **Демонстрационный режим**: автоматический сценарий с визуализацией работы GC
- **Интерактивный режим**: ручное управление объектами и памятью
- Создание объектов с указанием размера
- Управление ссылками между объектами
- Определение корневых объектов
- Автоматический запуск GC при нехватке памяти
- Визуализация состояния памяти

## Команды интерактивного режима
1. Создать объект
2. Добавить ссылку между объектами
3. Удалить корневой объект
4. Запустить сборщик мусора вручную
5. Показать текущее состояние памяти
0. Выход

## Структура проекта
- `MemoryObject` — класс объекта в памяти
- `MemoryManager` — менеджер памяти с реализацией GC
- `GarbageCollectorEmulator` — главный класс приложения

